name: SBOM

on:
  push:
    branches: [main]
  workflow_dispatch:

# Least-privilege : stub has no permissions. When implemented Sprint 1+,
# will require `contents: read` (+ `security-events: write` if publishing SARIF).
permissions: {}

jobs:
  sbom-stub:
    name: SBOM (stub)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "SBOM generation — activated Sprint 1+ (cyclonedx-py + npm audit)"
