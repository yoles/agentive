name: Security

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'  # Monday 06:00 UTC
  workflow_dispatch:

# Least-privilege : stub has no permissions. When implemented Sprint 1+,
# will require `contents: read` + `security-events: write` for SARIF upload.
permissions: {}

jobs:
  security-stub:
    name: Security scan (stub)
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Dependency vulnerability scanning — activated Sprint 1+ (trivy + dependabot)"
