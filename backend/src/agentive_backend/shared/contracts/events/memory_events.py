"""Memory chunk events — placeholder until M4 Memory Manager ships.

Filled by Epic 3 stories (3.1 — vector storage, 3.3 — TTL & archival, 3.5 —
push memory). The naming convention ``m4.chunk.*`` is fixed.
"""
