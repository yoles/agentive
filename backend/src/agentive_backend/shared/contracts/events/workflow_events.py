"""Workflow lifecycle events — placeholder until M3 Workflow Engine ships.

Filled by Epic 4 stories (4.1 — workflow creation, 4.2 — execution, 4.6 —
interrupt/resume). The naming convention ``workflow_engine.workflow.*`` is fixed.
"""
