"""Agent lifecycle events — placeholder until M2 Agent Registry ships.

Filled by Epic 2 stories (2.1 — template creation, 2.2 — configuration,
2.4 — instance vs template distinction). The naming convention
``m2.agent.*`` is fixed.
"""
