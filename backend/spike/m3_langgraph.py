"""Spike M3 LangGraph — Story 1.2 gating critique #1.

**Stub Sprint 0 / Story 1.1** — implementation complète dans Story 1.2 :
    - Workflow minimal 2 agents (Producer + Reviewer) + 1 quality gate
    - Checkpointing persisté dans Postgres (reprise post-crash)
    - Human-in-the-loop natif (suspend/resume)
    - Gating critique : si échec, pivoter l'abstraction avant Sprint 1
"""

from __future__ import annotations


def main() -> None:
    print("Spike M3 LangGraph — implémentation dans Story 1.2")


if __name__ == "__main__":
    main()
