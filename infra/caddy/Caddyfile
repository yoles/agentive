# Caddy prod config — auto-HTTPS Let's Encrypt OR Tailscale MagicDNS
# Ports standards 80/443
#
# Sprint 0 : stub — à compléter avec le domaine de prod en déploiement initial

{
    # Email pour Let's Encrypt (à configurer via env var ou update avant déploiement)
    email {$CADDY_LETS_ENCRYPT_EMAIL:admin@example.com}
    # Admin API désactivée
    admin off
}

# TODO : remplacer par le domaine de prod réel
# Exemple :
# agentive.example.com {
#     reverse_proxy /api/* backend:8000
#     reverse_proxy /sse/*  backend:8000 {
#         flush_interval -1
#     }
#     reverse_proxy frontend:5173
#     ...
# }

# Placeholder (dev self-signed par défaut si aucun domaine prod défini)
:80 {
    respond "Agentive prod config — configurer un domaine avant déploiement" 503
}
